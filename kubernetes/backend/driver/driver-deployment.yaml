apiVersion: apps/v1
kind: Deployment
metadata:
  name: esd-driver
  labels:
    app: esd-driver
spec:
  replicas: 1
  selector:
    matchLabels:
      app: esd-driver
  template:
    metadata:
      labels:
        app: esd-driver
    spec:
      containers:
        - name: esd-driver
          image: esd-driver:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 3006
          env:
            - name: PGUSER
              valueFrom:
                secretKeyRef:
                  key: username
                  name: postgres-app
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  key: password
                  name: postgres-app
          envFrom:
            - secretRef:
                name: app-environment
            - configMapRef:
                name: database-connection-strings
          resources:
            limits:
              cpu: 500m
              memory: 512Mi
            requests:
              cpu: 100m
              memory: 128Mi
