apiVersion: apps/v1
kind: Deployment
metadata:
  name: esd-notification
  labels:
    app: esd-notification
spec:
  replicas: 1
  selector:
    matchLabels:
      app: esd-notification
  template:
    metadata:
      labels:
        app: esd-notification
    spec:
      containers:
        - name: esd-notification
          image: esd-notification:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 3004
          envFrom:
            - configMapRef:
                name: app-environment
          resources:
            limits:
              cpu: 500m
              memory: 512Mi
            requests:
              cpu: 100m
              memory: 128Mi
