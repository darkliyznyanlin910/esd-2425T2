apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: esd-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: admin.esd.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: esd-admin-frontend
                port:
                  number: 80
    - host: customer.esd.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: esd-customer-frontend
                port:
                  number: 80
    - host: driver.esd.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: esd-driver-frontend
                port:
                  number: 80
    - host: api.esd.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: esd-nginx
                port:
                  number: 80
    - host: temporal.esd.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: temporal-service
                port:
                  number: 80
